.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Generated ROM byte arrays -- copyrighted content, never commit these.
src/*_rom.h

# Synced copy of the shared core (regenerate with ./sync-core.sh) -- the
# canonical source lives in ../include/gb and ../src, not here.
lib/gbcore/
